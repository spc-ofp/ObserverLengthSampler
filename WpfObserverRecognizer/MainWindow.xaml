<Window x:Class="WpfObserverRecognizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Length Frequency"
        WindowState="Maximized">
    <Viewbox>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>
            <Label Name="lblSpecies" Content="Species:" Grid.Row="0" Grid.Column="0" />
            <TextBlock Name="txtSpeciesCode" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center"/>
            <Label Name="lblLength" Content="Length:" Grid.Row="1" Grid.Column="0" />
            <TextBlock Name="txtLength" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center"/>
        </Grid>
    </Viewbox>    
</Window>
